#.strip() is a string method that removes leading and trailing whitespace (spaces, tabs, newlines) from a string.

text = "  some spaces around  "
stripped_text = text.strip()
print("Stripped Text:", stripped_text)