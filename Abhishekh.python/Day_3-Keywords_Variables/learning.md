#eyworkds are very very important in pyton.
name =  shrirang  <-- this is called string
name = "shrirang" <-- this is called variable.

Local variable - A variable that is declared inside function and can only be accessed within that 
                 function. 
                

global variable -  global variable is  one thats declared outside of all functions.
                 it can be accessed anywhere in the program (inside and outside fucntion)
                 to modify global variable inside function we must use global keyword.